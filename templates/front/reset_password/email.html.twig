<h1>Reinitialisation de mot de passe</h1>

<p>Bonjour,</p>

<p>Pour reinitialiser votre mot de passe, cliquez sur ce lien :</p>

<p>
    <a href="{{ url('app_reset_password', {token: resetToken.token}) }}">
        {{ url('app_reset_password', {token: resetToken.token}) }}
    </a>
</p>

<p>Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Si vous n'avez pas demande cette action, ignorez cet email.</p>
